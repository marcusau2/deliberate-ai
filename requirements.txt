# Deliberate AI - Core Dependencies
# Python 3.9+

# GUI Framework
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
PyQt6-sip==13.6.0

# LLM Integration
openai==1.12.0

# Text Processing
pyyaml==6.0.1
pypdf2==3.0.0

# HTTP & Search
requests==2.31.0
httpx==0.27.0  # Required for openai 1.12.0 compatibility

# Text-to-Speech (Kokoro)
# Note: kokoro 0.7.x supports Python 3.9-3.12, kokoro 0.9.x requires Python 3.10-3.13
# misaki is a dependency of kokoro for G2P (grapheme-to-phoneme)
# Using kokoro>=0.7.0 which will install compatible misaki automatically
kokoro>=0.7.0
torch>=2.0.0
scipy>=1.11.0
sounddevice>=0.4.6
soundfile>=0.12.0

# Utilities
loguru>=0.7.0
